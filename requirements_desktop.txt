# Study Assistant Desktop App — Python Requirements
# Install: pip install -r requirements_desktop.txt

# PDF parsing (Apple Silicon native)
PyMuPDF>=1.23.0

# API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Native macOS desktop window (uses WKWebView)
pywebview>=4.4.0

# Data validation
pydantic>=2.0.0
